# LoRA-backdoor research, pip dependencies for the notebooks.
#
# PyTorch is intentionally NOT listed here. Install it first, matched to your
# CUDA version (or use a CUDA-enabled base container like
# nvcr.io/nvidia/pytorch:25.11-py3). See README.md for details.

# HuggingFace stack
transformers>=4.44
datasets
peft>=0.11
trl>=0.9                # SFTConfig API
accelerate              # transitive via trl.SFTTrainer, listed explicitly
safetensors
bitsandbytes>=0.43      # required for load_in_4bit (QLoRA) throughout

# Unsloth (fast QLoRA training).
# On ARM64 hosts (e.g. DGX Spark) install with --no-deps to avoid clobbering
# the container's torch / bitsandbytes builds:
#     pip install --no-deps unsloth unsloth_zoo
unsloth

# Scientific / plotting
numpy
matplotlib
scikit-learn            # roc_auc_score in the detection notebooks

# Jupyter (only required if you don't already have it)
jupyterlab
